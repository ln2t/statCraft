# Example configuration for paired t-test
# Compares activation between two conditions within the same subjects
# (e.g., pre vs post treatment, session 1 vs session 2)

# Analysis type
analysis_type: paired

# Participant filtering (optional)
participant_label: null

# File pattern for finding images - should match both conditions
file_pattern: "**/*task-nback*space-MNI*stat-effect*.nii.gz"

# Paired test settings
# Defines how to pair images from different conditions
paired_test:
  pair_by: sub        # BIDS entity to pair by: "sub" (subject, default), "ses" (session), "run", etc.
  condition1: pre     # First condition (e.g., baseline, session-1)
  condition2: post    # Second condition (e.g., treatment, session-2)

# NOTES on pair_by:
# - "sub" (default): Pairs observations with the same subject ID
# - "ses": Pairs observations with the same session (useful if you want to compare 
#          how conditions differ within each session)
# - "run": Pairs observations with the same run number
# - Any other BIDS entity abbreviation or full name works too
#
# Files must contain BIDS-like key-value pairs in the filename
# (e.g., sub-001_ses-pre_*.nii.gz) for pairing to work

# Statistical inference settings
inference:
  alpha: 0.05
  height_threshold: 0.001
  cluster_threshold: 10
  two_sided: true
  corrections:
    - uncorrected
    - fdr
    - bonferroni
  run_permutation: false

# Atlas for cluster annotation
atlas: harvard_oxford

# Output settings
output:
  save_maps: true
  save_tables: true
  generate_report: true
  report_filename: paired_test_report.html

# Computational settings
n_jobs: 1
verbose: 1
